fragment allLendingStrategyProperties on LendingStrategy {
  id
  createdAt
  name
  symbol
  poolAddress
  underlying
  norm
  targetAPR
  allowedCollateral(where: { allowed: true }) {
    id
    contractAddress
    allowed
  vaults {
    ...allVaultProperties
  }
}
