query vaultsByOwner($owner: String) {
  vaults(where: { id: $owner }) {
    ...allVaultProperties
  }
}

query vaultsByOwnerForStrategy($owner: String, $strategy: String) {
  vaults(where: { id: $owner, strategy: $strategy }) {
    ...allVaultProperties
  }
}
