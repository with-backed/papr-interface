query vaultsByOwner($owner: ID) {
  vaults(where: { id: $owner }) {
    ...allVaultProperties
  }
}

query vaultsByOwnerForStrategy($owner: ID, $strategy: String) {
  vaults(where: { id: $owner, strategy: $strategy }) {
    ...allVaultProperties
  }
}
