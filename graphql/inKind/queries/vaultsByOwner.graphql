query vaultsByOwner($owner: ID) {
  vaults(where: { id: $owner }) {
    ...allVaultProperties
  }
}

query vaultsByOwnerForController($owner: ID, $controller: String) {
  vaults(where: { id: $owner, controller: $controller }) {
    ...allVaultProperties
  }
}
