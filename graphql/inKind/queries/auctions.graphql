query auctions {
  auctions {
    ...allAuctionProperties
  }
}

query auctionsByNFTOwner($nftOwner: Bytes) {
  auctions(
    where: { nftOwner: $nftOwner }
    orderBy: endPrice
    orderDirection: desc
  ) {
    ...allAuctionProperties
  }
}
