query activityByControllerAndVault($controllerId: String, $vault: String) {
  activities(
    where: { controller: $controllerId, vault: $vault }
    orderBy: timestamp
    orderDirection: desc
  ) {
    ...allActivityProperties
  }
}
